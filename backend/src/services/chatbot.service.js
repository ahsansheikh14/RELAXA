const generateChatReply = async (messages) => {
  // TODO: Send conversation to OpenAI/Gemini provider and return assistant reply.
  return {
    text: 'Chatbot integration placeholder.',
  };
};

export { generateChatReply };
